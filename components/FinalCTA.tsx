import { Clock,Mail,MessageCircle,MapPin } from 'lucide-react'
import Logo from './Logo'
import Reveal from './Reveal'
import { formatBrazilianPhone,whatsappUrl } from '@/lib/contact-utils.mjs'
import type { SiteSettings } from '@/lib/types'
export default function FinalCTA({settings}:{settings:SiteSettings}){const phone=settings.phone||settings.whatsapp_number;return <section id="contato" className="relative overflow-hidden border-t border-gold/20 bg-black py-10"><div className="absolute inset-0 bg-[radial-gradient(circle_at_10%_50%,rgba(244,194,29,.12),transparent_25%),radial-gradient(circle_at_90%_50%,rgba(244,194,29,.08),transparent_25%)]"/><div className="shell relative grid items-center gap-8 md:grid-cols-[.8fr_1.4fr_.8fr]"><Reveal direction="left"><Logo/></Reveal><Reveal delay={.08} className="text-center"><h2 className="display text-2xl sm:text-3xl">Vamos tirar seu projeto<br/><span className="text-gold">do papel?</span></h2><p className="mt-3 text-[10px] text-white/65">Fale agora com Wagner Paulino e solicite um orçamento sem compromisso.</p><a href={whatsappUrl(settings.whatsapp_number,settings.whatsapp_message)} target="_blank" rel="noopener noreferrer" className="mt-4 inline-flex items-center gap-3 rounded-full bg-[#20b85a] px-7 py-3 font-display text-base font-bold text-white transition hover:scale-105"><MessageCircle fill="currentColor" size={20}/>{formatBrazilianPhone(phone)}</a><div className="contact-details">{settings.email&&<a href={`mailto:${settings.email}`}><Mail/>{settings.email}</a>}{settings.address&&<span><MapPin/>{settings.address}</span>}{settings.business_hours&&<span><Clock/>{settings.business_hours}</span>}</div><p className="mt-2 flex items-center justify-center gap-1 text-[8px] uppercase text-white/45"><MapPin size={10} className="text-gold"/>Atendimento rápido e confiável</p></Reveal><Reveal direction="right" delay={.16}><p className="-rotate-3 font-serif text-2xl italic leading-tight text-white/75">Energia que<br/>conecta, qualidade<br/>que transforma.<span className="mt-2 block h-[3px] w-20 bg-gold"/></p></Reveal></div></section>}
