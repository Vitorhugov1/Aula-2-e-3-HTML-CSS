'use client'
import { useRef,useState } from 'react'
import Image from 'next/image'
import { AnimatePresence,motion } from 'framer-motion'
import { ArrowLeft,ArrowRight,ArrowUpRight,X } from 'lucide-react'
import Reveal from './Reveal'
const projects=[['Iluminação residencial','/images/project-residential.svg'],['Iluminação externa','/images/project-exterior.svg'],['Automação','/images/project-automation.svg'],['Quadros elétricos','/images/project-panel.svg'],['Instalações comerciais','/images/project-commercial.svg']]
export default function Projects(){const scroller=useRef<HTMLDivElement>(null);const [selected,setSelected]=useState<number|null>(null);const slide=(d:number)=>scroller.current?.scrollBy({left:d*360,behavior:'smooth'});return <section id="projetos" className="section-pad bg-ink"><div className="shell"><Reveal><div className="flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><div><div className="eyebrow">Projetos realizados</div><h2 className="display text-3xl sm:text-4xl lg:text-[40px]">Qualidade que<br/><span className="text-gold">transforma ambientes</span></h2></div><div className="flex gap-2"><button onClick={()=>slide(-1)} aria-label="Projetos anteriores" className="flex h-12 w-12 items-center justify-center border border-white/15 hover:border-gold hover:text-gold"><ArrowLeft size={18}/></button><button onClick={()=>slide(1)} aria-label="Próximos projetos" className="flex h-12 w-12 items-center justify-center border border-white/15 hover:border-gold hover:text-gold"><ArrowRight size={18}/></button></div></div></Reveal></div><div ref={scroller} className="hide-scrollbar mt-8 flex snap-x gap-4 overflow-x-auto px-[max(1.25rem,calc((100vw-1440px)/2+4rem))] pb-2">{projects.map(([title,img],i)=><motion.button key={title} onClick={()=>setSelected(i)} initial={{opacity:0,y:30}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.25}} transition={{delay:i*.06}} className="group relative aspect-[4/3] min-w-[82vw] snap-start overflow-hidden border border-white/10 text-left sm:min-w-[42vw] lg:min-w-[300px] xl:min-w-[330px]"><Image src={img} alt={title} fill sizes="(max-width:640px) 80vw,350px" className="object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-black/90 via-transparent to-transparent"/><span className="absolute right-5 top-5 flex h-9 w-9 items-center justify-center bg-black/60 text-gold"><ArrowUpRight size={16}/></span><div className="absolute inset-x-0 bottom-0 p-6"><span className="text-[9px] uppercase tracking-[.2em] text-gold">Projeto 0{i+1}</span><h3 className="mt-2 font-display text-lg font-bold uppercase">{title}</h3></div></motion.button>)}</div><div className="shell mt-5"><a href="#contato" className="outline-btn">Ver mais projetos <ArrowRight size={16}/></a></div><AnimatePresence>{selected!==null&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-[70] flex items-center justify-center bg-black/95 p-4 backdrop-blur-md"><button aria-label="Fechar" className="absolute right-6 top-6 z-10 flex h-12 w-12 items-center justify-center border border-white/20" onClick={()=>setSelected(null)}><X/></button><motion.div initial={{scale:.94}} animate={{scale:1}} onClick={e=>e.stopPropagation()} className="relative h-[80vh] w-full max-w-6xl"><Image src={projects[selected][1]} alt={projects[selected][0]} fill sizes="100vw" className="object-contain"/></motion.div></motion.div>}</AnimatePresence></section>}
