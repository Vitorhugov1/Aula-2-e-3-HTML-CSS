'use client'
import { useRef,useState } from 'react'
import Image from 'next/image'
import { AnimatePresence,motion } from 'framer-motion'
import { ArrowLeft,ArrowRight,X } from 'lucide-react'
import Reveal from './Reveal'
const projects=[['Iluminação residencial','/images/project-residential.svg'],['Iluminação comercial','/images/project-commercial.svg'],['Quadros elétricos','/images/project-panel.svg'],['Iluminação externa','/images/project-exterior.svg'],['Automação','/images/project-automation.svg']]
export default function Projects(){const scroller=useRef<HTMLDivElement>(null);const [selected,setSelected]=useState<number|null>(null);const slide=(d:number)=>scroller.current?.scrollBy({left:d*300,behavior:'smooth'});return <section id="projetos" className="section-pad border-t border-white/10 bg-black"><div className="shell"><Reveal><div className="eyebrow">Projetos realizados</div><h2 className="display text-3xl sm:text-4xl">Qualidade que<br/>transforma ambientes</h2><span className="mt-3 block h-[3px] w-12 bg-gold"/></Reveal><div className="relative mt-7"><button onClick={()=>slide(-1)} className="absolute -left-4 top-1/2 z-10 flex h-9 w-9 -translate-y-1/2 items-center justify-center rounded-full border border-white/50 bg-black/80"><ArrowLeft size={15}/></button><div ref={scroller} className="hide-scrollbar flex snap-x gap-4 overflow-x-auto">{projects.map(([title,img],i)=><button key={title} onClick={()=>setSelected(i)} className="relative aspect-[1.35/1] min-w-[78%] snap-start overflow-hidden rounded border border-white/15 sm:min-w-[46%] lg:min-w-[23.5%]"><Image src={img} alt={title} fill sizes="280px" className="object-cover transition duration-500 hover:scale-105"/><span className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/80 p-3 text-left text-[9px] font-bold uppercase">{title}</span></button>)}</div><button onClick={()=>slide(1)} className="absolute -right-4 top-1/2 z-10 flex h-9 w-9 -translate-y-1/2 items-center justify-center rounded-full border border-white/50 bg-black/80"><ArrowRight size={15}/></button></div><div className="mt-5 text-center"><a href="#contato" className="outline-btn">Ver mais projetos <ArrowRight size={12}/></a></div></div><AnimatePresence>{selected!==null&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-[70] flex items-center justify-center bg-black/95 p-4"><button className="absolute right-6 top-6" onClick={()=>setSelected(null)}><X/></button><div className="relative h-[80vh] w-full max-w-5xl"><Image src={projects[selected][1]} alt={projects[selected][0]} fill className="object-contain"/></div></motion.div>}</AnimatePresence></section>}
